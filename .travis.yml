language: php

sudo: false

php:
  - 7.1
  - 7.2
  - 7.3
  - nightly

matrix:
  fast_finish: true
  allow_failures:
    - php: nightly

install:
  - composer install

script:
  - ./vendor/bin/phpcs --standard=PSR2 src/
  # - ./vendor/bin/phpunit
